<template>
  <div class="base-b">
    <div>我是B组件（发布方）</div>
    <button @click="sendMsgFn">发送消息</button>
  </div>
</template>

<script>
import Bus from "../utils/EventBus";
export default {
  methods: {
    sendMsgFn() {
      // 3. 在B组件（发布方），触发事件的方式传递参数（发布消息）
      // 语法：$emit('事件名', 参数1, 参数2, ...)
      // '事件名'必须和A组件中监听的'事件名'一致，否则不会触发
      // 发布消息是一个一对多的关系
      Bus.$emit("sendMsg", "今天天气不错，适合旅游");
    },
  },
};
</script>

<style scoped>
.base-b {
  width: 200px;
  height: 200px;
  border: 3px solid #000;
  border-radius: 3px;
  margin: 10px;
}
</style>